<snippet>
  <content>
<![CDATA[
import React, {
    PureComponent,
} from 'react';
import PropTypes from 'prop-types';
import {
    View,
    Modal,
    TextInput,
    TouchableOpacity,
    Image,
} from 'react-native';
import style from './style';
import Navbar from 'buss/Navbar';
import Text from 'buss/Text';

class ${1:Index} extends PureComponent {
    static propTypes = {
        $2
    };
    constructor(props) {
        super(props);

        this.state = {
            $3
        };
    }
    componentDidMount() {
        this._isMounted = true;
    }
    componentWillUnmount() {
        this._isMounted = false;
    }
    render() {
        return <View style={style.box}>
            $0
        </View>
    }
}

export default $1;

]]>
  </content>
  <tabTrigger>createRNClass</tabTrigger>
  <scope>source.js,source.jsx,source.css</scope>
  <description>创建RNClass Remi</description>
</snippet>
